console.log([1, 2, 3] + [4, 5]);

/*
** will output a merged array - however the last element will be the [4, 5] array
*/

// got it wrong - array merges into string but with no separation at the point of merge