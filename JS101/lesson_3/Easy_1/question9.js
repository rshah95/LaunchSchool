let flintstones = ["Fred", "Barney", "Wilma", "Betty", "Bambam", "Pebbles"];
flintstones.push('Dino', 'Hoppy')

console.log(flintstones);

// array.push() can have multiple arguments but ig array passed in - array is added rather than individual components