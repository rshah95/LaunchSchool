let str1 = "Come over here!"; // true
let str2 = "What's up, Doc?"; // false

if ('!' === str1.substring(str1.length - 1)) {
  print ('string ends in exclamation point.')
}

// could have also used string.endsWith()