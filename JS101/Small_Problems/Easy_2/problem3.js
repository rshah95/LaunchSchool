function multiply(num1, num2) {
  return num1 * num2
}

console.log(multiply(5, 3) === 15);

// could have also written (on one line) as arrow function

/*
** let multiply = (num1, num2) ==> num1 * num2; 
*/